# sp-poc

Run `nix develop` to use the committed flake

## rust-toolchain

If needed, update the rust version in the `rust-toolchain.toml` file. This is read by the flake to determine which rust toolchain to build.
